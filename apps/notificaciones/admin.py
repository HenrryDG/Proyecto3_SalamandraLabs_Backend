"""
Admin para notificaciones.

Como las notificaciones no tienen modelo de base de datos,
este archivo está vacío pero se mantiene por consistencia
con la estructura de otras apps Django.
"""

from django.contrib import admin

# No hay modelos que registrar ya que las notificaciones
# se generan dinámicamente sin persistencia en base de datos.
